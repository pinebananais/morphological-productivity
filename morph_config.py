# list of suffixes to trace
suffix_table = ['ly', 'ous', 'ment'] # can append new suffixes

# file name store raw data(in relative path)
file_name = "./data/sample_raw_data.txt"

# flag for using original method based on hapax legomenon (True for use)
flag_1 = True # [True | False]

# flag for using new method based on exponential function (True for use)
flag_2 = True # [True | False]